<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="16dp">

        <!-- Preview Frame -->
        <FrameLayout
            android:layout_width="200dp"
            android:layout_height="350dp"
            android:layout_marginTop="20dp"
            android:background="@drawable/preview_frame">

            <ImageView
                android:id="@+id/previewImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitCenter"
                android:contentDescription="Preview" />

            <TextView
                android:id="@+id/noPreviewText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/status_no_preview"
                android:textAlignment="center"
                android:textColor="#888888"
                android:visibility="visible" />
        </FrameLayout>

        <!-- Virtual Display Status -->
        <TextView
            android:id="@+id/vdStatusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="@string/status_vd_waiting"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Ink Screen Info -->
        <TextView
            android:id="@+id/inkScreenInfoText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@string/status_not_connected"
            android:textColor="#888888"
            android:textSize="12sp" />

        <!-- USB Status -->
        <TextView
            android:id="@+id/usbStatusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/status_usb_disconnected"
            android:textColor="@color/status_red"
            android:textSize="12sp" />

        <!-- Mode Selection -->
        <RadioGroup
            android:id="@+id/modeRadioGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="24dp"
            android:layout_marginTop="16dp"
            android:orientation="vertical">

            <RadioButton
                android:id="@+id/radioMirror"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="镜像模式 (默认)"
                android:checked="true" />

            <RadioButton
                android:id="@+id/radioTaskMigration"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="任务迁移模式" />

            <RadioButton
                android:id="@+id/radioExtended"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="独立扩展屏模式" />
        </RadioGroup>

        <!-- Toggle Projection Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/toggleButton"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_marginTop="32dp"
            android:layout_marginHorizontal="24dp"
            android:text="@string/btn_start_projection"
            android:textSize="18sp"
            app:cornerRadius="32dp"
            app:icon="@android:drawable/ic_media_play"
            app:iconSize="24dp"
            app:iconPadding="12dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="自动化握手已开启"
            android:textColor="#BBBBBB"
            android:textSize="12sp" />

    </LinearLayout>
</ScrollView>
